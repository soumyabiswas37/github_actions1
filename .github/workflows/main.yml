name: "hello world"

on:
    push:
        branches:
            - master
            - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: "Print Hello"
              run: echo "Hello World"

            - name: "Printing Date"
              run: echo $(date)
